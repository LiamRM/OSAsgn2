#ifndef SELECTIONSORT_H
#define SELECTIONSORT_H

#include "taxpayer.h"
#include <string>

void t_selection_sort(Taxpayer* tarray[], int attributeNum, int n, int startIndex, int endIndex, std::string order);

#endif