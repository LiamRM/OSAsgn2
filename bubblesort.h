#ifndef BUBBLESORT_H
#define BUBBLESORT_H

#include <string>
#include "taxpayer.h"

void t_bubble_sort(Taxpayer* tarray[], int attributeNum, int n, int startIndex, int endIndex, std::string order);

#endif